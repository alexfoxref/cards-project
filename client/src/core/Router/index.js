import { setConfig } from './config'
import { usePath, useMatch, useLocation, useHistory } from './hooks'
import { Router, Link, Switch, Route, Redirect, useRouter } from './components'

export {
  Router,
  Link,
  Switch,
  Route,
  Redirect,
  useRouter,
  useMatch,
  useLocation,
  useHistory,
  setConfig,
  usePath,
}
